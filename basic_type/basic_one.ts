let a: string;
let b: boolean;
let c: string;

type User = {
  name: string;
  surname: string;
  age: number;
  isAdmin: boolean;
};

const user: User = {
  name: "John",
  surname: "Smith",
  age: 30,
  isAdmin: true,
};

const mul = (a: number,b: number):number => a + b;

const add = (a: number,b: number):number => a + b;